name: Madcap Deals Monitor

on:
  schedule:
    # Run every 5 minutes, offset from common high-load cron slots.
    - cron: '2,7,12,17,22,27,32,37,42,47,52,57 * * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  run-monitor:
    concurrency:
      group: madcap-monitor
      cancel-in-progress: false

    runs-on: ubuntu-latest
    permissions:
      contents: write # Allows pushing the updated products.json back

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --omit=dev

      - name: Run monitor
        env:
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: node monitor.js --once

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add products.json
          if git commit -m "chore: update products database [skip ci]"; then
            git push
          else
            echo "No changes to commit"
          fi
